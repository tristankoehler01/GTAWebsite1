
@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    @foreach (var item in Model.Course)
    {
        <div style="align-content:center; width:250px; height:100px; border:solid; border-width:5px; text-align:center; background-color:#005293; border-color:#FECB00; color:white">@Html.DisplayFor(modelItem => item.courseName)</div>
        <br>
    }
</div>
